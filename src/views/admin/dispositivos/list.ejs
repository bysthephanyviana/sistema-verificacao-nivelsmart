<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Painel Administrativo - Dispositivos - NivelSmart</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="admin-body">
    <div class="admin-container">
        <header style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem;">
            <div style="display: flex; align-items: center; gap: 15px;">
                <img src="/logo.png" alt="NivelSmart" style="max-width: 120px;">
                <h1 style="margin: 0;">Dispositivos</h1>
            </div>
            <div style="display: flex; gap: 10px;">
                <a href="/admin" class="btn" style="background: #eee;">Técnicos</a>
                <a href="/admin/dispositivos/novo" class="btn btn-primary">+ Novo Dispositivo</a>
                <a href="/logout" class="btn" style="background: var(--danger); color: white;">Sair</a>
            </div>
        </header>

        <div class="card" style="text-align: left; padding: 0;">
            <div class="table-responsive">
                <table>
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nome</th>
                            <th>ID Service</th>
                            <th>Condomínio</th>
                            <th>Estoque</th>
                            <th>Status</th>
                            <th>Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% dispositivos.forEach(dispositivo => { %>
                            <tr>
                                <td><strong><%= dispositivo.id %></strong></td>
                                <td><%= dispositivo.nome %></td>
                                <td><%= dispositivo.id_service %></td>
                                <td><%= dispositivo.condominio %></td>
                                <td><%= dispositivo.quantidade_estoque %></td>
                                <td>
                                    <span style="padding: 4px 8px; border-radius: 4px; font-size: 0.8rem; font-weight: 700; background: <%= dispositivo.status === 'ATIVO' ? '#dff9fb' : '#ffeaa7' %>; color: <%= dispositivo.status === 'ATIVO' ? '#0984e3' : '#d63031' %>">
                                        <%= dispositivo.status %>
                                    </span>
                                </td>
                                <td>
                                    <a href="/admin/dispositivos/editar/<%= dispositivo.id %>" style="color: #666; margin-right: 10px;">Editar</a>
                                    <a href="javascript:void(0)" onclick="showQR('<%= dispositivo.id %>')" style="color: var(--primary); margin-right: 10px;">QR Code</a>
                                    <a href="/admin/dispositivos/deletar/<%= dispositivo.id %>" style="color: var(--danger);" onclick="return confirm('Tem certeza que deseja excluir este dispositivo? Esta ação é permanente.')">Excluir</a>
                                </td>
                            </tr>
                        <% }) %>
                        <% if (dispositivos.length === 0) { %>
                            <tr>
                                <td colspan="7" style="text-align: center; padding: 2rem;">Nenhum dispositivo encontrado.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Modal QR Code -->
    <div id="qrOverlay" style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.5); z-index:999;" onclick="closeQR()"></div>
    <div id="qrModal" style="display:none; position:fixed; top:50%; left:50%; transform:translate(-50%, -50%); background:white; padding:2rem; border-radius:10px; z-index:1000; text-align:center;">
        <h3>QR Code do Dispositivo</h3>
        <p id="qrId" style="font-weight: 700; margin: 10px 0;"></p>
        <img id="qrImg" src="" style="width: 200px; height: 200px; margin: 20px 0;">
        <div style="display: flex; gap: 10px; justify-content: center;">
            <button onclick="downloadQR()" class="btn btn-primary" id="btnDownload">Download</button>
            <button onclick="closeQR()" class="btn" style="background: #eee;">Fechar</button>
        </div>
    </div>

    <script>
        function showQR(id) {
            document.getElementById('qrId').innerText = 'ID: ' + id;
            fetch('/api/dispositivo/qrcode/' + id)
                .then(res => res.json())
                .then(data => {
                    document.getElementById('qrImg').src = data.qrDataUrl;
                    document.getElementById('qrOverlay').style.display = 'block';
                    document.getElementById('qrModal').style.display = 'block';
                });
        }

        function closeQR() {
            document.getElementById('qrOverlay').style.display = 'none';
            document.getElementById('qrModal').style.display = 'none';
        }

        function downloadQR() {
            const link = document.createElement('a');
            link.href = document.getElementById('qrImg').src;
            link.download = 'qrcode-dispositivo-' + document.getElementById('qrId').innerText.replace('ID: ', '') + '.png';
            link.click();
        }
    </script>
</body>
</html>
